# --- API ---------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Validation / config ----------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Data processing ---------------------------------------------------
pandas==2.2.3
numpy==2.2.1
openpyxl==3.1.5   # .xlsx reader
xlrd==2.0.1       # legacy .xls reader

# --- Persistence -------------------------------------------------------
SQLAlchemy==2.0.36
# Uncomment for PostgreSQL. SQLite (stdlib) is the local-dev default.
# psycopg[binary]==3.2.3

# --- HTTP client for LLM providers ------------------------------------
httpx==0.28.1

# --- Tests / benchmark -------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
psutil==6.1.1
